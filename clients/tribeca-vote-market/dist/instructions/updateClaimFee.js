/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU16Decoder, getU16Encoder, transformEncoder, } from "@solana/kit";
import { VOTE_MARKET_PROGRAM_ADDRESS } from "../programs/index.js";
import { getAccountMetaFactory } from "../shared/index.js";
export const UPDATE_CLAIM_FEE_DISCRIMINATOR = new Uint8Array([
    192, 255, 140, 184, 150, 96, 181, 103,
]);
export function getUpdateClaimFeeDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_CLAIM_FEE_DISCRIMINATOR);
}
export function getUpdateClaimFeeInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["claimFee", getU16Encoder()],
    ]), (value) => ({ ...value, discriminator: UPDATE_CLAIM_FEE_DISCRIMINATOR }));
}
export function getUpdateClaimFeeInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["claimFee", getU16Decoder()],
    ]);
}
export function getUpdateClaimFeeInstructionDataCodec() {
    return combineCodec(getUpdateClaimFeeInstructionDataEncoder(), getUpdateClaimFeeInstructionDataDecoder());
}
export function getUpdateClaimFeeInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? VOTE_MARKET_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        config: { value: input.config ?? null, isWritable: true },
        admin: { value: input.admin ?? null, isWritable: false },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [getAccountMeta(accounts.config), getAccountMeta(accounts.admin)],
        data: getUpdateClaimFeeInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function parseUpdateClaimFeeInstruction(instruction) {
    if (instruction.accounts.length < 2) {
        // TODO: Coded error.
        throw new Error("Not enough accounts");
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: { config: getNextAccount(), admin: getNextAccount() },
        data: getUpdateClaimFeeInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=updateClaimFee.js.map