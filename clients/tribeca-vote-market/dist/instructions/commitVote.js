/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, transformEncoder, } from "@solana/kit";
import { VOTE_MARKET_PROGRAM_ADDRESS } from "../programs/index.js";
import { getAccountMetaFactory } from "../shared/index.js";
export const COMMIT_VOTE_DISCRIMINATOR = new Uint8Array([
    134, 97, 90, 126, 91, 66, 16, 26,
]);
export function getCommitVoteDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(COMMIT_VOTE_DISCRIMINATOR);
}
export function getCommitVoteInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["epoch", getU32Encoder()],
    ]), (value) => ({ ...value, discriminator: COMMIT_VOTE_DISCRIMINATOR }));
}
export function getCommitVoteInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["epoch", getU32Decoder()],
    ]);
}
export function getCommitVoteInstructionDataCodec() {
    return combineCodec(getCommitVoteInstructionDataEncoder(), getCommitVoteInstructionDataDecoder());
}
export function getCommitVoteInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? VOTE_MARKET_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        config: { value: input.config ?? null, isWritable: false },
        scriptAuthority: { value: input.scriptAuthority ?? null, isWritable: true },
        gaugemeister: { value: input.gaugemeister ?? null, isWritable: false },
        gauge: { value: input.gauge ?? null, isWritable: false },
        gaugeVoter: { value: input.gaugeVoter ?? null, isWritable: false },
        gaugeVote: { value: input.gaugeVote ?? null, isWritable: false },
        epochGauge: { value: input.epochGauge ?? null, isWritable: true },
        epochGaugeVoter: { value: input.epochGaugeVoter ?? null, isWritable: true },
        epochGaugeVote: { value: input.epochGaugeVote ?? null, isWritable: true },
        voteDelegate: { value: input.voteDelegate ?? null, isWritable: true },
        gaugeProgram: { value: input.gaugeProgram ?? null, isWritable: false },
        systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    // Resolve default values.
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value =
            "11111111111111111111111111111111";
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [
            getAccountMeta(accounts.config),
            getAccountMeta(accounts.scriptAuthority),
            getAccountMeta(accounts.gaugemeister),
            getAccountMeta(accounts.gauge),
            getAccountMeta(accounts.gaugeVoter),
            getAccountMeta(accounts.gaugeVote),
            getAccountMeta(accounts.epochGauge),
            getAccountMeta(accounts.epochGaugeVoter),
            getAccountMeta(accounts.epochGaugeVote),
            getAccountMeta(accounts.voteDelegate),
            getAccountMeta(accounts.gaugeProgram),
            getAccountMeta(accounts.systemProgram),
        ],
        data: getCommitVoteInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function parseCommitVoteInstruction(instruction) {
    if (instruction.accounts.length < 12) {
        // TODO: Coded error.
        throw new Error("Not enough accounts");
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            config: getNextAccount(),
            scriptAuthority: getNextAccount(),
            gaugemeister: getNextAccount(),
            gauge: getNextAccount(),
            gaugeVoter: getNextAccount(),
            gaugeVote: getNextAccount(),
            epochGauge: getNextAccount(),
            epochGaugeVoter: getNextAccount(),
            epochGaugeVote: getNextAccount(),
            voteDelegate: getNextAccount(),
            gaugeProgram: getNextAccount(),
            systemProgram: getNextAccount(),
        },
        data: getCommitVoteInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=commitVote.js.map