/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU32Decoder, getU32Encoder, getU64Decoder, getU64Encoder, transformEncoder, } from "@solana/kit";
import { VOTE_MARKET_PROGRAM_ADDRESS } from "../programs/index.js";
import { getAccountMetaFactory } from "../shared/index.js";
export const SET_MAX_AMOUNT_DISCRIMINATOR = new Uint8Array([
    226, 133, 252, 54, 185, 15, 122, 154,
]);
export function getSetMaxAmountDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(SET_MAX_AMOUNT_DISCRIMINATOR);
}
export function getSetMaxAmountInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["epoch", getU32Encoder()],
        ["maxAmount", getU64Encoder()],
    ]), (value) => ({ ...value, discriminator: SET_MAX_AMOUNT_DISCRIMINATOR }));
}
export function getSetMaxAmountInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["epoch", getU32Decoder()],
        ["maxAmount", getU64Decoder()],
    ]);
}
export function getSetMaxAmountInstructionDataCodec() {
    return combineCodec(getSetMaxAmountInstructionDataEncoder(), getSetMaxAmountInstructionDataDecoder());
}
export function getSetMaxAmountInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? VOTE_MARKET_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        config: { value: input.config ?? null, isWritable: false },
        voteBuy: { value: input.voteBuy ?? null, isWritable: true },
        gauge: { value: input.gauge ?? null, isWritable: false },
        scriptAuthority: {
            value: input.scriptAuthority ?? null,
            isWritable: false,
        },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [
            getAccountMeta(accounts.config),
            getAccountMeta(accounts.voteBuy),
            getAccountMeta(accounts.gauge),
            getAccountMeta(accounts.scriptAuthority),
        ],
        data: getSetMaxAmountInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function parseSetMaxAmountInstruction(instruction) {
    if (instruction.accounts.length < 4) {
        // TODO: Coded error.
        throw new Error("Not enough accounts");
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            config: getNextAccount(),
            voteBuy: getNextAccount(),
            gauge: getNextAccount(),
            scriptAuthority: getNextAccount(),
        },
        data: getSetMaxAmountInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=setMaxAmount.js.map