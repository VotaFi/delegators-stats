/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getArrayDecoder, getArrayEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, transformEncoder, } from "@solana/kit";
import { VOTE_MARKET_PROGRAM_ADDRESS } from "../programs/index.js";
import { getAccountMetaFactory } from "../shared/index.js";
export const UPDATE_ALLOWED_MINTS_DISCRIMINATOR = new Uint8Array([
    146, 211, 161, 2, 10, 20, 94, 67,
]);
export function getUpdateAllowedMintsDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_ALLOWED_MINTS_DISCRIMINATOR);
}
export function getUpdateAllowedMintsInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["allowedMints", getArrayEncoder(getAddressEncoder())],
    ]), (value) => ({
        ...value,
        discriminator: UPDATE_ALLOWED_MINTS_DISCRIMINATOR,
    }));
}
export function getUpdateAllowedMintsInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["allowedMints", getArrayDecoder(getAddressDecoder())],
    ]);
}
export function getUpdateAllowedMintsInstructionDataCodec() {
    return combineCodec(getUpdateAllowedMintsInstructionDataEncoder(), getUpdateAllowedMintsInstructionDataDecoder());
}
export function getUpdateAllowedMintsInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? VOTE_MARKET_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        config: { value: input.config ?? null, isWritable: true },
        admin: { value: input.admin ?? null, isWritable: true },
        allowedMints: { value: input.allowedMints ?? null, isWritable: true },
        systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input, allowedMints: input.allowedMintsArg };
    // Resolve default values.
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value =
            "11111111111111111111111111111111";
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [
            getAccountMeta(accounts.config),
            getAccountMeta(accounts.admin),
            getAccountMeta(accounts.allowedMints),
            getAccountMeta(accounts.systemProgram),
        ],
        data: getUpdateAllowedMintsInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function parseUpdateAllowedMintsInstruction(instruction) {
    if (instruction.accounts.length < 4) {
        // TODO: Coded error.
        throw new Error("Not enough accounts");
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            config: getNextAccount(),
            admin: getNextAccount(),
            allowedMints: getNextAccount(),
            systemProgram: getNextAccount(),
        },
        data: getUpdateAllowedMintsInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=updateAllowedMints.js.map