/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, transformEncoder, } from "@solana/kit";
import { VOTE_MARKET_PROGRAM_ADDRESS } from "../programs/index.js";
import { getAccountMetaFactory } from "../shared/index.js";
export const UPDATE_REWARD_ACCUMULATOR_CONFIG_DISCRIMINATOR = new Uint8Array([
    212, 118, 126, 80, 89, 132, 184, 215,
]);
export function getUpdateRewardAccumulatorConfigDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_REWARD_ACCUMULATOR_CONFIG_DISCRIMINATOR);
}
export function getUpdateRewardAccumulatorConfigInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["rewardAccumulatorProgram", getAddressEncoder()],
        ["namespace", fixEncoderSize(getBytesEncoder(), 8)],
    ]), (value) => ({
        ...value,
        discriminator: UPDATE_REWARD_ACCUMULATOR_CONFIG_DISCRIMINATOR,
    }));
}
export function getUpdateRewardAccumulatorConfigInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["rewardAccumulatorProgram", getAddressDecoder()],
        ["namespace", fixDecoderSize(getBytesDecoder(), 8)],
    ]);
}
export function getUpdateRewardAccumulatorConfigInstructionDataCodec() {
    return combineCodec(getUpdateRewardAccumulatorConfigInstructionDataEncoder(), getUpdateRewardAccumulatorConfigInstructionDataDecoder());
}
export function getUpdateRewardAccumulatorConfigInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? VOTE_MARKET_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        config: { value: input.config ?? null, isWritable: false },
        rewardAccumulatorConfig: {
            value: input.rewardAccumulatorConfig ?? null,
            isWritable: true,
        },
        admin: { value: input.admin ?? null, isWritable: true },
        systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    // Resolve default values.
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value =
            "11111111111111111111111111111111";
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [
            getAccountMeta(accounts.config),
            getAccountMeta(accounts.rewardAccumulatorConfig),
            getAccountMeta(accounts.admin),
            getAccountMeta(accounts.systemProgram),
        ],
        data: getUpdateRewardAccumulatorConfigInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function parseUpdateRewardAccumulatorConfigInstruction(instruction) {
    if (instruction.accounts.length < 4) {
        // TODO: Coded error.
        throw new Error("Not enough accounts");
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            config: getNextAccount(),
            rewardAccumulatorConfig: getNextAccount(),
            admin: getNextAccount(),
            systemProgram: getNextAccount(),
        },
        data: getUpdateRewardAccumulatorConfigInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=updateRewardAccumulatorConfig.js.map