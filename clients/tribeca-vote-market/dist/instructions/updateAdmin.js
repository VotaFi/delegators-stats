/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, transformEncoder, } from "@solana/kit";
import { VOTE_MARKET_PROGRAM_ADDRESS } from "../programs/index.js";
import { getAccountMetaFactory } from "../shared/index.js";
export const UPDATE_ADMIN_DISCRIMINATOR = new Uint8Array([
    161, 176, 40, 213, 60, 184, 179, 228,
]);
export function getUpdateAdminDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_ADMIN_DISCRIMINATOR);
}
export function getUpdateAdminInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["admin", getAddressEncoder()],
    ]), (value) => ({ ...value, discriminator: UPDATE_ADMIN_DISCRIMINATOR }));
}
export function getUpdateAdminInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["admin", getAddressDecoder()],
    ]);
}
export function getUpdateAdminInstructionDataCodec() {
    return combineCodec(getUpdateAdminInstructionDataEncoder(), getUpdateAdminInstructionDataDecoder());
}
export function getUpdateAdminInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? VOTE_MARKET_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        config: { value: input.config ?? null, isWritable: true },
        admin: { value: input.admin ?? null, isWritable: false },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input, admin: input.adminArg };
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [getAccountMeta(accounts.config), getAccountMeta(accounts.admin)],
        data: getUpdateAdminInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function parseUpdateAdminInstruction(instruction) {
    if (instruction.accounts.length < 2) {
        // TODO: Coded error.
        throw new Error("Not enough accounts");
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: { config: getNextAccount(), admin: getNextAccount() },
        data: getUpdateAdminInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=updateAdmin.js.map