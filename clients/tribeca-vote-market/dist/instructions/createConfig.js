/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getArrayDecoder, getArrayEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU16Decoder, getU16Encoder, transformEncoder, } from "@solana/kit";
import { VOTE_MARKET_PROGRAM_ADDRESS } from "../programs/index.js";
import { getAccountMetaFactory } from "../shared/index.js";
export const CREATE_CONFIG_DISCRIMINATOR = new Uint8Array([
    201, 207, 243, 114, 75, 111, 47, 189,
]);
export function getCreateConfigDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(CREATE_CONFIG_DISCRIMINATOR);
}
export function getCreateConfigInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["mints", getArrayEncoder(getAddressEncoder())],
        ["claimFee", getU16Encoder()],
        ["scriptAuthority", getAddressEncoder()],
    ]), (value) => ({ ...value, discriminator: CREATE_CONFIG_DISCRIMINATOR }));
}
export function getCreateConfigInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["mints", getArrayDecoder(getAddressDecoder())],
        ["claimFee", getU16Decoder()],
        ["scriptAuthority", getAddressDecoder()],
    ]);
}
export function getCreateConfigInstructionDataCodec() {
    return combineCodec(getCreateConfigInstructionDataEncoder(), getCreateConfigInstructionDataDecoder());
}
export function getCreateConfigInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? VOTE_MARKET_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        config: { value: input.config ?? null, isWritable: true },
        gaugemeister: { value: input.gaugemeister ?? null, isWritable: false },
        payer: { value: input.payer ?? null, isWritable: true },
        allowedMints: { value: input.allowedMints ?? null, isWritable: true },
        systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    // Resolve default values.
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value =
            "11111111111111111111111111111111";
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [
            getAccountMeta(accounts.config),
            getAccountMeta(accounts.gaugemeister),
            getAccountMeta(accounts.payer),
            getAccountMeta(accounts.allowedMints),
            getAccountMeta(accounts.systemProgram),
        ],
        data: getCreateConfigInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function parseCreateConfigInstruction(instruction) {
    if (instruction.accounts.length < 5) {
        // TODO: Coded error.
        throw new Error("Not enough accounts");
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            config: getNextAccount(),
            gaugemeister: getNextAccount(),
            payer: getNextAccount(),
            allowedMints: getNextAccount(),
            systemProgram: getNextAccount(),
        },
        data: getCreateConfigInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=createConfig.js.map