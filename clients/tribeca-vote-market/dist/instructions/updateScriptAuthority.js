/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, transformEncoder, } from "@solana/kit";
import { VOTE_MARKET_PROGRAM_ADDRESS } from "../programs/index.js";
import { getAccountMetaFactory } from "../shared/index.js";
export const UPDATE_SCRIPT_AUTHORITY_DISCRIMINATOR = new Uint8Array([
    57, 237, 52, 47, 112, 6, 101, 72,
]);
export function getUpdateScriptAuthorityDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(UPDATE_SCRIPT_AUTHORITY_DISCRIMINATOR);
}
export function getUpdateScriptAuthorityInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["scriptAuthority", getAddressEncoder()],
    ]), (value) => ({
        ...value,
        discriminator: UPDATE_SCRIPT_AUTHORITY_DISCRIMINATOR,
    }));
}
export function getUpdateScriptAuthorityInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["scriptAuthority", getAddressDecoder()],
    ]);
}
export function getUpdateScriptAuthorityInstructionDataCodec() {
    return combineCodec(getUpdateScriptAuthorityInstructionDataEncoder(), getUpdateScriptAuthorityInstructionDataDecoder());
}
export function getUpdateScriptAuthorityInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? VOTE_MARKET_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        config: { value: input.config ?? null, isWritable: true },
        admin: { value: input.admin ?? null, isWritable: false },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [getAccountMeta(accounts.config), getAccountMeta(accounts.admin)],
        data: getUpdateScriptAuthorityInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function parseUpdateScriptAuthorityInstruction(instruction) {
    if (instruction.accounts.length < 2) {
        // TODO: Coded error.
        throw new Error("Not enough accounts");
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: { config: getNextAccount(), admin: getNextAccount() },
        data: getUpdateScriptAuthorityInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=updateScriptAuthority.js.map