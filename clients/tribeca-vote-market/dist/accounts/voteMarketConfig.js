/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU16Decoder, getU16Encoder, transformEncoder, } from "@solana/kit";
export const VOTE_MARKET_CONFIG_DISCRIMINATOR = new Uint8Array([
    172, 230, 34, 183, 39, 247, 10, 193,
]);
export function getVoteMarketConfigDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(VOTE_MARKET_CONFIG_DISCRIMINATOR);
}
export function getVoteMarketConfigEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["scriptAuthority", getAddressEncoder()],
        ["gaugemeister", getAddressEncoder()],
        ["admin", getAddressEncoder()],
        ["claimFee", getU16Encoder()],
    ]), (value) => ({ ...value, discriminator: VOTE_MARKET_CONFIG_DISCRIMINATOR }));
}
export function getVoteMarketConfigDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["scriptAuthority", getAddressDecoder()],
        ["gaugemeister", getAddressDecoder()],
        ["admin", getAddressDecoder()],
        ["claimFee", getU16Decoder()],
    ]);
}
export function getVoteMarketConfigCodec() {
    return combineCodec(getVoteMarketConfigEncoder(), getVoteMarketConfigDecoder());
}
export function decodeVoteMarketConfig(encodedAccount) {
    return decodeAccount(encodedAccount, getVoteMarketConfigDecoder());
}
export async function fetchVoteMarketConfig(rpc, address, config) {
    const maybeAccount = await fetchMaybeVoteMarketConfig(rpc, address, config);
    assertAccountExists(maybeAccount);
    return maybeAccount;
}
export async function fetchMaybeVoteMarketConfig(rpc, address, config) {
    const maybeAccount = await fetchEncodedAccount(rpc, address, config);
    return decodeVoteMarketConfig(maybeAccount);
}
export async function fetchAllVoteMarketConfig(rpc, addresses, config) {
    const maybeAccounts = await fetchAllMaybeVoteMarketConfig(rpc, addresses, config);
    assertAccountsExist(maybeAccounts);
    return maybeAccounts;
}
export async function fetchAllMaybeVoteMarketConfig(rpc, addresses, config) {
    const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
    return maybeAccounts.map((maybeAccount) => decodeVoteMarketConfig(maybeAccount));
}
export function getVoteMarketConfigSize() {
    return 106;
}
//# sourceMappingURL=voteMarketConfig.js.map