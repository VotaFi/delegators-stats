/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getOptionDecoder, getOptionEncoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, transformEncoder, } from "@solana/kit";
export const VOTE_BUY_DISCRIMINATOR = new Uint8Array([
    155, 17, 20, 82, 247, 189, 203, 225,
]);
export function getVoteBuyDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(VOTE_BUY_DISCRIMINATOR);
}
export function getVoteBuyEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["gauge", getAddressEncoder()],
        ["mint", getAddressEncoder()],
        ["amount", getU64Encoder()],
        ["maxAmount", getOptionEncoder(getU64Encoder())],
        ["buyer", getAddressEncoder()],
    ]), (value) => ({ ...value, discriminator: VOTE_BUY_DISCRIMINATOR }));
}
export function getVoteBuyDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["gauge", getAddressDecoder()],
        ["mint", getAddressDecoder()],
        ["amount", getU64Decoder()],
        ["maxAmount", getOptionDecoder(getU64Decoder())],
        ["buyer", getAddressDecoder()],
    ]);
}
export function getVoteBuyCodec() {
    return combineCodec(getVoteBuyEncoder(), getVoteBuyDecoder());
}
export function decodeVoteBuy(encodedAccount) {
    return decodeAccount(encodedAccount, getVoteBuyDecoder());
}
export async function fetchVoteBuy(rpc, address, config) {
    const maybeAccount = await fetchMaybeVoteBuy(rpc, address, config);
    assertAccountExists(maybeAccount);
    return maybeAccount;
}
export async function fetchMaybeVoteBuy(rpc, address, config) {
    const maybeAccount = await fetchEncodedAccount(rpc, address, config);
    return decodeVoteBuy(maybeAccount);
}
export async function fetchAllVoteBuy(rpc, addresses, config) {
    const maybeAccounts = await fetchAllMaybeVoteBuy(rpc, addresses, config);
    assertAccountsExist(maybeAccounts);
    return maybeAccounts;
}
export async function fetchAllMaybeVoteBuy(rpc, addresses, config) {
    const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
    return maybeAccounts.map((maybeAccount) => decodeVoteBuy(maybeAccount));
}
//# sourceMappingURL=voteBuy.js.map