/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, transformEncoder, } from "@solana/kit";
export const REWARD_ACCUMULATOR_CONFIG_DISCRIMINATOR = new Uint8Array([
    121, 30, 187, 126, 122, 174, 181, 210,
]);
export function getRewardAccumulatorConfigDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(REWARD_ACCUMULATOR_CONFIG_DISCRIMINATOR);
}
export function getRewardAccumulatorConfigEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["rewardAccumulatorProgram", getAddressEncoder()],
        ["namespace", fixEncoderSize(getBytesEncoder(), 8)],
    ]), (value) => ({
        ...value,
        discriminator: REWARD_ACCUMULATOR_CONFIG_DISCRIMINATOR,
    }));
}
export function getRewardAccumulatorConfigDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["rewardAccumulatorProgram", getAddressDecoder()],
        ["namespace", fixDecoderSize(getBytesDecoder(), 8)],
    ]);
}
export function getRewardAccumulatorConfigCodec() {
    return combineCodec(getRewardAccumulatorConfigEncoder(), getRewardAccumulatorConfigDecoder());
}
export function decodeRewardAccumulatorConfig(encodedAccount) {
    return decodeAccount(encodedAccount, getRewardAccumulatorConfigDecoder());
}
export async function fetchRewardAccumulatorConfig(rpc, address, config) {
    const maybeAccount = await fetchMaybeRewardAccumulatorConfig(rpc, address, config);
    assertAccountExists(maybeAccount);
    return maybeAccount;
}
export async function fetchMaybeRewardAccumulatorConfig(rpc, address, config) {
    const maybeAccount = await fetchEncodedAccount(rpc, address, config);
    return decodeRewardAccumulatorConfig(maybeAccount);
}
export async function fetchAllRewardAccumulatorConfig(rpc, addresses, config) {
    const maybeAccounts = await fetchAllMaybeRewardAccumulatorConfig(rpc, addresses, config);
    assertAccountsExist(maybeAccounts);
    return maybeAccounts;
}
export async function fetchAllMaybeRewardAccumulatorConfig(rpc, addresses, config) {
    const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
    return maybeAccounts.map((maybeAccount) => decodeRewardAccumulatorConfig(maybeAccount));
}
export function getRewardAccumulatorConfigSize() {
    return 48;
}
//# sourceMappingURL=rewardAccumulatorConfig.js.map