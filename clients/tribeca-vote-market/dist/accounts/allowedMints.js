/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getArrayDecoder, getArrayEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, transformEncoder, } from "@solana/kit";
export const ALLOWED_MINTS_DISCRIMINATOR = new Uint8Array([
    207, 250, 95, 68, 245, 220, 110, 31,
]);
export function getAllowedMintsDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(ALLOWED_MINTS_DISCRIMINATOR);
}
export function getAllowedMintsEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["mints", getArrayEncoder(getAddressEncoder())],
    ]), (value) => ({ ...value, discriminator: ALLOWED_MINTS_DISCRIMINATOR }));
}
export function getAllowedMintsDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["mints", getArrayDecoder(getAddressDecoder())],
    ]);
}
export function getAllowedMintsCodec() {
    return combineCodec(getAllowedMintsEncoder(), getAllowedMintsDecoder());
}
export function decodeAllowedMints(encodedAccount) {
    return decodeAccount(encodedAccount, getAllowedMintsDecoder());
}
export async function fetchAllowedMints(rpc, address, config) {
    const maybeAccount = await fetchMaybeAllowedMints(rpc, address, config);
    assertAccountExists(maybeAccount);
    return maybeAccount;
}
export async function fetchMaybeAllowedMints(rpc, address, config) {
    const maybeAccount = await fetchEncodedAccount(rpc, address, config);
    return decodeAllowedMints(maybeAccount);
}
export async function fetchAllAllowedMints(rpc, addresses, config) {
    const maybeAccounts = await fetchAllMaybeAllowedMints(rpc, addresses, config);
    assertAccountsExist(maybeAccounts);
    return maybeAccounts;
}
export async function fetchAllMaybeAllowedMints(rpc, addresses, config) {
    const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
    return maybeAccounts.map((maybeAccount) => decodeAllowedMints(maybeAccount));
}
//# sourceMappingURL=allowedMints.js.map