/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU8Decoder, getU8Encoder, transformEncoder, } from "@solana/kit";
export const LOCKER_WHITELIST_ENTRY_DISCRIMINATOR = new Uint8Array([
    128, 245, 238, 138, 226, 48, 216, 63,
]);
export function getLockerWhitelistEntryDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(LOCKER_WHITELIST_ENTRY_DISCRIMINATOR);
}
export function getLockerWhitelistEntryEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["bump", getU8Encoder()],
        ["locker", getAddressEncoder()],
        ["programId", getAddressEncoder()],
        ["owner", getAddressEncoder()],
    ]), (value) => ({
        ...value,
        discriminator: LOCKER_WHITELIST_ENTRY_DISCRIMINATOR,
    }));
}
export function getLockerWhitelistEntryDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["bump", getU8Decoder()],
        ["locker", getAddressDecoder()],
        ["programId", getAddressDecoder()],
        ["owner", getAddressDecoder()],
    ]);
}
export function getLockerWhitelistEntryCodec() {
    return combineCodec(getLockerWhitelistEntryEncoder(), getLockerWhitelistEntryDecoder());
}
export function decodeLockerWhitelistEntry(encodedAccount) {
    return decodeAccount(encodedAccount, getLockerWhitelistEntryDecoder());
}
export async function fetchLockerWhitelistEntry(rpc, address, config) {
    const maybeAccount = await fetchMaybeLockerWhitelistEntry(rpc, address, config);
    assertAccountExists(maybeAccount);
    return maybeAccount;
}
export async function fetchMaybeLockerWhitelistEntry(rpc, address, config) {
    const maybeAccount = await fetchEncodedAccount(rpc, address, config);
    return decodeLockerWhitelistEntry(maybeAccount);
}
export async function fetchAllLockerWhitelistEntry(rpc, addresses, config) {
    const maybeAccounts = await fetchAllMaybeLockerWhitelistEntry(rpc, addresses, config);
    assertAccountsExist(maybeAccounts);
    return maybeAccounts;
}
export async function fetchAllMaybeLockerWhitelistEntry(rpc, addresses, config) {
    const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
    return maybeAccounts.map((maybeAccount) => decodeLockerWhitelistEntry(maybeAccount));
}
export function getLockerWhitelistEntrySize() {
    return 105;
}
//# sourceMappingURL=lockerWhitelistEntry.js.map