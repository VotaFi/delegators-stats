/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { assertAccountExists, assertAccountsExist, combineCodec, decodeAccount, fetchEncodedAccount, fetchEncodedAccounts, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU8Decoder, getU8Encoder, getU64Decoder, getU64Encoder, transformEncoder, } from "@solana/kit";
import { getLockerParamsDecoder, getLockerParamsEncoder, } from "../types/index.js";
export const LOCKER_DISCRIMINATOR = new Uint8Array([
    74, 246, 6, 113, 249, 228, 75, 169,
]);
export function getLockerDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(LOCKER_DISCRIMINATOR);
}
export function getLockerEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["base", getAddressEncoder()],
        ["bump", getU8Encoder()],
        ["tokenMint", getAddressEncoder()],
        ["lockedSupply", getU64Encoder()],
        ["governor", getAddressEncoder()],
        ["params", getLockerParamsEncoder()],
    ]), (value) => ({ ...value, discriminator: LOCKER_DISCRIMINATOR }));
}
export function getLockerDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["base", getAddressDecoder()],
        ["bump", getU8Decoder()],
        ["tokenMint", getAddressDecoder()],
        ["lockedSupply", getU64Decoder()],
        ["governor", getAddressDecoder()],
        ["params", getLockerParamsDecoder()],
    ]);
}
export function getLockerCodec() {
    return combineCodec(getLockerEncoder(), getLockerDecoder());
}
export function decodeLocker(encodedAccount) {
    return decodeAccount(encodedAccount, getLockerDecoder());
}
export async function fetchLocker(rpc, address, config) {
    const maybeAccount = await fetchMaybeLocker(rpc, address, config);
    assertAccountExists(maybeAccount);
    return maybeAccount;
}
export async function fetchMaybeLocker(rpc, address, config) {
    const maybeAccount = await fetchEncodedAccount(rpc, address, config);
    return decodeLocker(maybeAccount);
}
export async function fetchAllLocker(rpc, addresses, config) {
    const maybeAccounts = await fetchAllMaybeLocker(rpc, addresses, config);
    assertAccountsExist(maybeAccounts);
    return maybeAccounts;
}
export async function fetchAllMaybeLocker(rpc, addresses, config) {
    const maybeAccounts = await fetchEncodedAccounts(rpc, addresses, config);
    return maybeAccounts.map((maybeAccount) => decodeLocker(maybeAccount));
}
export function getLockerSize() {
    return 139;
}
//# sourceMappingURL=locker.js.map