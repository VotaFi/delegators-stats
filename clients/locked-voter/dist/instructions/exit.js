/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, transformEncoder, } from "@solana/kit";
import { LOCKED_VOTER_PROGRAM_ADDRESS } from "../programs/lockedVoter.js";
import { getAccountMetaFactory } from "../shared/index.js";
export const EXIT_DISCRIMINATOR = new Uint8Array([
    234, 32, 12, 71, 126, 5, 219, 160,
]);
export function getExitDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(EXIT_DISCRIMINATOR);
}
export function getExitInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({ ...value, discriminator: EXIT_DISCRIMINATOR }));
}
export function getExitInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ]);
}
export function getExitInstructionDataCodec() {
    return combineCodec(getExitInstructionDataEncoder(), getExitInstructionDataDecoder());
}
export function getExitInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? LOCKED_VOTER_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        locker: { value: input.locker ?? null, isWritable: true },
        escrow: { value: input.escrow ?? null, isWritable: true },
        escrowOwner: { value: input.escrowOwner ?? null, isWritable: false },
        escrowTokens: { value: input.escrowTokens ?? null, isWritable: true },
        destinationTokens: {
            value: input.destinationTokens ?? null,
            isWritable: true,
        },
        payer: { value: input.payer ?? null, isWritable: true },
        tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    };
    const accounts = originalAccounts;
    // Resolve default values.
    if (!accounts.tokenProgram.value) {
        accounts.tokenProgram.value =
            "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA";
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [
            getAccountMeta(accounts.locker),
            getAccountMeta(accounts.escrow),
            getAccountMeta(accounts.escrowOwner),
            getAccountMeta(accounts.escrowTokens),
            getAccountMeta(accounts.destinationTokens),
            getAccountMeta(accounts.payer),
            getAccountMeta(accounts.tokenProgram),
        ],
        data: getExitInstructionDataEncoder().encode({}),
        programAddress,
    });
}
export function parseExitInstruction(instruction) {
    if (instruction.accounts.length < 7) {
        // TODO: Coded error.
        throw new Error("Not enough accounts");
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            locker: getNextAccount(),
            escrow: getNextAccount(),
            escrowOwner: getNextAccount(),
            escrowTokens: getNextAccount(),
            destinationTokens: getNextAccount(),
            payer: getNextAccount(),
            tokenProgram: getNextAccount(),
        },
        data: getExitInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=exit.js.map