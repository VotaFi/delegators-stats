/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU8Decoder, getU8Encoder, transformEncoder, } from "@solana/kit";
import { LOCKED_VOTER_PROGRAM_ADDRESS } from "../programs/lockedVoter.js";
import { getAccountMetaFactory } from "../shared/index.js";
export const APPROVE_PROGRAM_LOCK_PRIVILEGE_DISCRIMINATOR = new Uint8Array([
    75, 202, 1, 4, 122, 110, 102, 148,
]);
export function getApproveProgramLockPrivilegeDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(APPROVE_PROGRAM_LOCK_PRIVILEGE_DISCRIMINATOR);
}
export function getApproveProgramLockPrivilegeInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["bump", getU8Encoder()],
    ]), (value) => ({
        ...value,
        discriminator: APPROVE_PROGRAM_LOCK_PRIVILEGE_DISCRIMINATOR,
    }));
}
export function getApproveProgramLockPrivilegeInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["bump", getU8Decoder()],
    ]);
}
export function getApproveProgramLockPrivilegeInstructionDataCodec() {
    return combineCodec(getApproveProgramLockPrivilegeInstructionDataEncoder(), getApproveProgramLockPrivilegeInstructionDataDecoder());
}
export function getApproveProgramLockPrivilegeInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? LOCKED_VOTER_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        locker: { value: input.locker ?? null, isWritable: false },
        whitelistEntry: { value: input.whitelistEntry ?? null, isWritable: true },
        governor: { value: input.governor ?? null, isWritable: false },
        smartWallet: { value: input.smartWallet ?? null, isWritable: false },
        executableId: { value: input.executableId ?? null, isWritable: false },
        whitelistedOwner: {
            value: input.whitelistedOwner ?? null,
            isWritable: false,
        },
        payer: { value: input.payer ?? null, isWritable: true },
        systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    // Resolve default values.
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value =
            "11111111111111111111111111111111";
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [
            getAccountMeta(accounts.locker),
            getAccountMeta(accounts.whitelistEntry),
            getAccountMeta(accounts.governor),
            getAccountMeta(accounts.smartWallet),
            getAccountMeta(accounts.executableId),
            getAccountMeta(accounts.whitelistedOwner),
            getAccountMeta(accounts.payer),
            getAccountMeta(accounts.systemProgram),
        ],
        data: getApproveProgramLockPrivilegeInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function parseApproveProgramLockPrivilegeInstruction(instruction) {
    if (instruction.accounts.length < 8) {
        // TODO: Coded error.
        throw new Error("Not enough accounts");
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            locker: getNextAccount(),
            whitelistEntry: getNextAccount(),
            governor: getNextAccount(),
            smartWallet: getNextAccount(),
            executableId: getNextAccount(),
            whitelistedOwner: getNextAccount(),
            payer: getNextAccount(),
            systemProgram: getNextAccount(),
        },
        data: getApproveProgramLockPrivilegeInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=approveProgramLockPrivilege.js.map