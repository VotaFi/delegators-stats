/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, transformEncoder, } from "@solana/kit";
import { LOCKED_VOTER_PROGRAM_ADDRESS } from "../programs/lockedVoter.js";
import { getAccountMetaFactory } from "../shared/index.js";
export const REVOKE_PROGRAM_LOCK_PRIVILEGE_DISCRIMINATOR = new Uint8Array([
    170, 151, 7, 88, 194, 86, 245, 112,
]);
export function getRevokeProgramLockPrivilegeDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(REVOKE_PROGRAM_LOCK_PRIVILEGE_DISCRIMINATOR);
}
export function getRevokeProgramLockPrivilegeInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([["discriminator", fixEncoderSize(getBytesEncoder(), 8)]]), (value) => ({
        ...value,
        discriminator: REVOKE_PROGRAM_LOCK_PRIVILEGE_DISCRIMINATOR,
    }));
}
export function getRevokeProgramLockPrivilegeInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
    ]);
}
export function getRevokeProgramLockPrivilegeInstructionDataCodec() {
    return combineCodec(getRevokeProgramLockPrivilegeInstructionDataEncoder(), getRevokeProgramLockPrivilegeInstructionDataDecoder());
}
export function getRevokeProgramLockPrivilegeInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? LOCKED_VOTER_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        locker: { value: input.locker ?? null, isWritable: false },
        whitelistEntry: { value: input.whitelistEntry ?? null, isWritable: true },
        governor: { value: input.governor ?? null, isWritable: false },
        smartWallet: { value: input.smartWallet ?? null, isWritable: false },
        payer: { value: input.payer ?? null, isWritable: true },
    };
    const accounts = originalAccounts;
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [
            getAccountMeta(accounts.locker),
            getAccountMeta(accounts.whitelistEntry),
            getAccountMeta(accounts.governor),
            getAccountMeta(accounts.smartWallet),
            getAccountMeta(accounts.payer),
        ],
        data: getRevokeProgramLockPrivilegeInstructionDataEncoder().encode({}),
        programAddress,
    });
}
export function parseRevokeProgramLockPrivilegeInstruction(instruction) {
    if (instruction.accounts.length < 5) {
        // TODO: Coded error.
        throw new Error("Not enough accounts");
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            locker: getNextAccount(),
            whitelistEntry: getNextAccount(),
            governor: getNextAccount(),
            smartWallet: getNextAccount(),
            payer: getNextAccount(),
        },
        data: getRevokeProgramLockPrivilegeInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=revokeProgramLockPrivilege.js.map