/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getI64Decoder, getI64Encoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, transformEncoder, } from "@solana/kit";
import { LOCKED_VOTER_PROGRAM_ADDRESS } from "../programs/lockedVoter.js";
import { getAccountMetaFactory } from "../shared/index.js";
export const LOCK_PERMISSIONLESS_DISCRIMINATOR = new Uint8Array([
    146, 106, 208, 36, 187, 241, 122, 2,
]);
export function getLockPermissionlessDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(LOCK_PERMISSIONLESS_DISCRIMINATOR);
}
export function getLockPermissionlessInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["amount", getU64Encoder()],
        ["duration", getI64Encoder()],
    ]), (value) => ({ ...value, discriminator: LOCK_PERMISSIONLESS_DISCRIMINATOR }));
}
export function getLockPermissionlessInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["amount", getU64Decoder()],
        ["duration", getI64Decoder()],
    ]);
}
export function getLockPermissionlessInstructionDataCodec() {
    return combineCodec(getLockPermissionlessInstructionDataEncoder(), getLockPermissionlessInstructionDataDecoder());
}
export function getLockPermissionlessInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? LOCKED_VOTER_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        locker: { value: input.locker ?? null, isWritable: true },
        escrow: { value: input.escrow ?? null, isWritable: true },
        escrowTokens: { value: input.escrowTokens ?? null, isWritable: true },
        escrowOwner: { value: input.escrowOwner ?? null, isWritable: false },
        sourceTokens: { value: input.sourceTokens ?? null, isWritable: true },
        tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    // Resolve default values.
    if (!accounts.tokenProgram.value) {
        accounts.tokenProgram.value =
            "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA";
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [
            getAccountMeta(accounts.locker),
            getAccountMeta(accounts.escrow),
            getAccountMeta(accounts.escrowTokens),
            getAccountMeta(accounts.escrowOwner),
            getAccountMeta(accounts.sourceTokens),
            getAccountMeta(accounts.tokenProgram),
        ],
        data: getLockPermissionlessInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function parseLockPermissionlessInstruction(instruction) {
    if (instruction.accounts.length < 6) {
        // TODO: Coded error.
        throw new Error("Not enough accounts");
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            locker: getNextAccount(),
            escrow: getNextAccount(),
            escrowTokens: getNextAccount(),
            escrowOwner: getNextAccount(),
            sourceTokens: getNextAccount(),
            tokenProgram: getNextAccount(),
        },
        data: getLockPermissionlessInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=lockPermissionless.js.map