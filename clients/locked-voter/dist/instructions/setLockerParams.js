/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, transformEncoder, } from "@solana/kit";
import { LOCKED_VOTER_PROGRAM_ADDRESS } from "../programs/lockedVoter.js";
import { getAccountMetaFactory } from "../shared/index.js";
import { getLockerParamsDecoder, getLockerParamsEncoder, } from "../types/index.js";
export const SET_LOCKER_PARAMS_DISCRIMINATOR = new Uint8Array([
    106, 39, 132, 84, 254, 77, 161, 169,
]);
export function getSetLockerParamsDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(SET_LOCKER_PARAMS_DISCRIMINATOR);
}
export function getSetLockerParamsInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["params", getLockerParamsEncoder()],
    ]), (value) => ({ ...value, discriminator: SET_LOCKER_PARAMS_DISCRIMINATOR }));
}
export function getSetLockerParamsInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["params", getLockerParamsDecoder()],
    ]);
}
export function getSetLockerParamsInstructionDataCodec() {
    return combineCodec(getSetLockerParamsInstructionDataEncoder(), getSetLockerParamsInstructionDataDecoder());
}
export function getSetLockerParamsInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? LOCKED_VOTER_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        locker: { value: input.locker ?? null, isWritable: true },
        governor: { value: input.governor ?? null, isWritable: false },
        smartWallet: { value: input.smartWallet ?? null, isWritable: false },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [
            getAccountMeta(accounts.locker),
            getAccountMeta(accounts.governor),
            getAccountMeta(accounts.smartWallet),
        ],
        data: getSetLockerParamsInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function parseSetLockerParamsInstruction(instruction) {
    if (instruction.accounts.length < 3) {
        // TODO: Coded error.
        throw new Error("Not enough accounts");
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            locker: getNextAccount(),
            governor: getNextAccount(),
            smartWallet: getNextAccount(),
        },
        data: getSetLockerParamsInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=setLockerParams.js.map