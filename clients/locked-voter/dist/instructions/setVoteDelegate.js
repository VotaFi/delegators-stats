/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getAddressDecoder, getAddressEncoder, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, transformEncoder, } from "@solana/kit";
import { LOCKED_VOTER_PROGRAM_ADDRESS } from "../programs/lockedVoter.js";
import { getAccountMetaFactory } from "../shared/index.js";
export const SET_VOTE_DELEGATE_DISCRIMINATOR = new Uint8Array([
    46, 236, 241, 243, 251, 108, 156, 12,
]);
export function getSetVoteDelegateDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(SET_VOTE_DELEGATE_DISCRIMINATOR);
}
export function getSetVoteDelegateInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["newDelegate", getAddressEncoder()],
    ]), (value) => ({ ...value, discriminator: SET_VOTE_DELEGATE_DISCRIMINATOR }));
}
export function getSetVoteDelegateInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["newDelegate", getAddressDecoder()],
    ]);
}
export function getSetVoteDelegateInstructionDataCodec() {
    return combineCodec(getSetVoteDelegateInstructionDataEncoder(), getSetVoteDelegateInstructionDataDecoder());
}
export function getSetVoteDelegateInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? LOCKED_VOTER_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        escrow: { value: input.escrow ?? null, isWritable: true },
        escrowOwner: { value: input.escrowOwner ?? null, isWritable: false },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [
            getAccountMeta(accounts.escrow),
            getAccountMeta(accounts.escrowOwner),
        ],
        data: getSetVoteDelegateInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function parseSetVoteDelegateInstruction(instruction) {
    if (instruction.accounts.length < 2) {
        // TODO: Coded error.
        throw new Error("Not enough accounts");
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: { escrow: getNextAccount(), escrowOwner: getNextAccount() },
        data: getSetVoteDelegateInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=setVoteDelegate.js.map