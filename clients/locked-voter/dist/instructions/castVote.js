/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU8Decoder, getU8Encoder, transformEncoder, } from "@solana/kit";
import { LOCKED_VOTER_PROGRAM_ADDRESS } from "../programs/lockedVoter.js";
import { getAccountMetaFactory } from "../shared/index.js";
export const CAST_VOTE_DISCRIMINATOR = new Uint8Array([
    20, 212, 15, 189, 69, 180, 69, 151,
]);
export function getCastVoteDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(CAST_VOTE_DISCRIMINATOR);
}
export function getCastVoteInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["side", getU8Encoder()],
    ]), (value) => ({ ...value, discriminator: CAST_VOTE_DISCRIMINATOR }));
}
export function getCastVoteInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["side", getU8Decoder()],
    ]);
}
export function getCastVoteInstructionDataCodec() {
    return combineCodec(getCastVoteInstructionDataEncoder(), getCastVoteInstructionDataDecoder());
}
export function getCastVoteInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? LOCKED_VOTER_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        locker: { value: input.locker ?? null, isWritable: false },
        escrow: { value: input.escrow ?? null, isWritable: false },
        voteDelegate: { value: input.voteDelegate ?? null, isWritable: false },
        proposal: { value: input.proposal ?? null, isWritable: true },
        vote: { value: input.vote ?? null, isWritable: true },
        governor: { value: input.governor ?? null, isWritable: false },
        governProgram: { value: input.governProgram ?? null, isWritable: false },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [
            getAccountMeta(accounts.locker),
            getAccountMeta(accounts.escrow),
            getAccountMeta(accounts.voteDelegate),
            getAccountMeta(accounts.proposal),
            getAccountMeta(accounts.vote),
            getAccountMeta(accounts.governor),
            getAccountMeta(accounts.governProgram),
        ],
        data: getCastVoteInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function parseCastVoteInstruction(instruction) {
    if (instruction.accounts.length < 7) {
        // TODO: Coded error.
        throw new Error("Not enough accounts");
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            locker: getNextAccount(),
            escrow: getNextAccount(),
            voteDelegate: getNextAccount(),
            proposal: getNextAccount(),
            vote: getNextAccount(),
            governor: getNextAccount(),
            governProgram: getNextAccount(),
        },
        data: getCastVoteInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=castVote.js.map