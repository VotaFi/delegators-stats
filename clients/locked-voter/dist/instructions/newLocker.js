/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getStructDecoder, getStructEncoder, getU8Decoder, getU8Encoder, transformEncoder, } from "@solana/kit";
import { LOCKED_VOTER_PROGRAM_ADDRESS } from "../programs/lockedVoter.js";
import { getAccountMetaFactory } from "../shared/index.js";
import { getLockerParamsDecoder, getLockerParamsEncoder, } from "../types/index.js";
export const NEW_LOCKER_DISCRIMINATOR = new Uint8Array([
    177, 133, 32, 90, 229, 216, 131, 47,
]);
export function getNewLockerDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(NEW_LOCKER_DISCRIMINATOR);
}
export function getNewLockerInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["bump", getU8Encoder()],
        ["params", getLockerParamsEncoder()],
    ]), (value) => ({ ...value, discriminator: NEW_LOCKER_DISCRIMINATOR }));
}
export function getNewLockerInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["bump", getU8Decoder()],
        ["params", getLockerParamsDecoder()],
    ]);
}
export function getNewLockerInstructionDataCodec() {
    return combineCodec(getNewLockerInstructionDataEncoder(), getNewLockerInstructionDataDecoder());
}
export function getNewLockerInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? LOCKED_VOTER_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        base: { value: input.base ?? null, isWritable: false },
        locker: { value: input.locker ?? null, isWritable: true },
        tokenMint: { value: input.tokenMint ?? null, isWritable: false },
        governor: { value: input.governor ?? null, isWritable: false },
        payer: { value: input.payer ?? null, isWritable: true },
        systemProgram: { value: input.systemProgram ?? null, isWritable: false },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    // Resolve default values.
    if (!accounts.systemProgram.value) {
        accounts.systemProgram.value =
            "11111111111111111111111111111111";
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [
            getAccountMeta(accounts.base),
            getAccountMeta(accounts.locker),
            getAccountMeta(accounts.tokenMint),
            getAccountMeta(accounts.governor),
            getAccountMeta(accounts.payer),
            getAccountMeta(accounts.systemProgram),
        ],
        data: getNewLockerInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function parseNewLockerInstruction(instruction) {
    if (instruction.accounts.length < 6) {
        // TODO: Coded error.
        throw new Error("Not enough accounts");
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            base: getNextAccount(),
            locker: getNextAccount(),
            tokenMint: getNextAccount(),
            governor: getNextAccount(),
            payer: getNextAccount(),
            systemProgram: getNextAccount(),
        },
        data: getNewLockerInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=newLocker.js.map