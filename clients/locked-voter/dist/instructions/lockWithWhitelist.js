/**
 * This code was AUTOGENERATED using the Codama library.
 * Please DO NOT EDIT THIS FILE, instead use visitors
 * to add features, then rerun Codama to update it.
 *
 * @see https://github.com/codama-idl/codama
 */
import { combineCodec, fixDecoderSize, fixEncoderSize, getBytesDecoder, getBytesEncoder, getI64Decoder, getI64Encoder, getStructDecoder, getStructEncoder, getU64Decoder, getU64Encoder, transformEncoder, } from "@solana/kit";
import { LOCKED_VOTER_PROGRAM_ADDRESS } from "../programs/lockedVoter.js";
import { getAccountMetaFactory } from "../shared/index.js";
export const LOCK_WITH_WHITELIST_DISCRIMINATOR = new Uint8Array([
    138, 141, 28, 193, 7, 211, 181, 69,
]);
export function getLockWithWhitelistDiscriminatorBytes() {
    return fixEncoderSize(getBytesEncoder(), 8).encode(LOCK_WITH_WHITELIST_DISCRIMINATOR);
}
export function getLockWithWhitelistInstructionDataEncoder() {
    return transformEncoder(getStructEncoder([
        ["discriminator", fixEncoderSize(getBytesEncoder(), 8)],
        ["amount", getU64Encoder()],
        ["duration", getI64Encoder()],
    ]), (value) => ({ ...value, discriminator: LOCK_WITH_WHITELIST_DISCRIMINATOR }));
}
export function getLockWithWhitelistInstructionDataDecoder() {
    return getStructDecoder([
        ["discriminator", fixDecoderSize(getBytesDecoder(), 8)],
        ["amount", getU64Decoder()],
        ["duration", getI64Decoder()],
    ]);
}
export function getLockWithWhitelistInstructionDataCodec() {
    return combineCodec(getLockWithWhitelistInstructionDataEncoder(), getLockWithWhitelistInstructionDataDecoder());
}
export function getLockWithWhitelistInstruction(input, config) {
    // Program address.
    const programAddress = config?.programAddress ?? LOCKED_VOTER_PROGRAM_ADDRESS;
    // Original accounts.
    const originalAccounts = {
        locker: { value: input.locker ?? null, isWritable: true },
        escrow: { value: input.escrow ?? null, isWritable: true },
        escrowTokens: { value: input.escrowTokens ?? null, isWritable: true },
        escrowOwner: { value: input.escrowOwner ?? null, isWritable: false },
        sourceTokens: { value: input.sourceTokens ?? null, isWritable: true },
        tokenProgram: { value: input.tokenProgram ?? null, isWritable: false },
        instructionsSysvar: {
            value: input.instructionsSysvar ?? null,
            isWritable: false,
        },
    };
    const accounts = originalAccounts;
    // Original args.
    const args = { ...input };
    // Resolve default values.
    if (!accounts.tokenProgram.value) {
        accounts.tokenProgram.value =
            "TokenkegQfeZyiNwAJbNbGKPFXCWuBvf9Ss623VQ5DA";
    }
    if (!accounts.instructionsSysvar.value) {
        accounts.instructionsSysvar.value =
            "Sysvar1nstructions1111111111111111111111111";
    }
    const getAccountMeta = getAccountMetaFactory(programAddress, "programId");
    return Object.freeze({
        accounts: [
            getAccountMeta(accounts.locker),
            getAccountMeta(accounts.escrow),
            getAccountMeta(accounts.escrowTokens),
            getAccountMeta(accounts.escrowOwner),
            getAccountMeta(accounts.sourceTokens),
            getAccountMeta(accounts.tokenProgram),
            getAccountMeta(accounts.instructionsSysvar),
        ],
        data: getLockWithWhitelistInstructionDataEncoder().encode(args),
        programAddress,
    });
}
export function parseLockWithWhitelistInstruction(instruction) {
    if (instruction.accounts.length < 7) {
        // TODO: Coded error.
        throw new Error("Not enough accounts");
    }
    let accountIndex = 0;
    const getNextAccount = () => {
        const accountMeta = instruction.accounts[accountIndex];
        accountIndex += 1;
        return accountMeta;
    };
    return {
        programAddress: instruction.programAddress,
        accounts: {
            locker: getNextAccount(),
            escrow: getNextAccount(),
            escrowTokens: getNextAccount(),
            escrowOwner: getNextAccount(),
            sourceTokens: getNextAccount(),
            tokenProgram: getNextAccount(),
            instructionsSysvar: getNextAccount(),
        },
        data: getLockWithWhitelistInstructionDataDecoder().decode(instruction.data),
    };
}
//# sourceMappingURL=lockWithWhitelist.js.map